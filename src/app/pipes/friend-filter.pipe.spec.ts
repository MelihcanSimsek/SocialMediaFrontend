import { FriendFilterPipe } from './friend-filter.pipe';

describe('FriendFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
